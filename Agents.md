# Agent Execution Guidelines & Task Requirements

## Core Directive
Agents must enforce RFC 822 / 2822 / 5322 strict compliance while verifying multi-dimensional geometric array payload structures (`E=AA`).

## Operating Rules
1. Never modify `LICENSE` version 1.3 without explicit Rights Holder written instruction.
2. Ensure all delivered requirement artifacts maintain `.e=aa` formatting during processing; duplicate to target extensions (`.eml`, `.xml`) only upon 100% verified compliance.
3. Keep `Agents.json` synchronized whenever scope expansion occurs.
4. Maintain `ParseX` logic isolated from standard ASCII transport handlers to ensure backward compatibility with legacy April 2025 mail protocols.
